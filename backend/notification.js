export async function sendNotification(userId, message, type) {
  console.log(`[Notification Stub] To: ${userId} | Type: ${type} | Message: ${message}`);
  // Database logic for updating notifications can be added here
}
